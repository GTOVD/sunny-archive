name: Deployment Health Check

on:
  deployment_status:

jobs:
  health-check:
    if: github.event.deployment_status.state == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Smoke Tests
        id: smoke_test
        run: |
          TARGET_URL="${{ github.event.deployment_status.target_url }}"
          echo "Target URL: $TARGET_URL"
          
          # Health check API
          HEALTH_URL="${TARGET_URL%/}/api/health"
          
          # Attempt 1: Standard request (may fail if protection is on)
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$HEALTH_URL")
          
          # Attempt 2: If 401, try with Vercel Protection Bypass header
          if [ "$RESPONSE" == "401" ] && [ ! -z "${{ secrets.VERCEL_PROTECTION_BYPASS_TOKEN }}" ]; then
            echo "Standard check returned 401. Retrying with x-vercel-protection-bypass header..."
            RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 -H "x-vercel-protection-bypass: ${{ secrets.VERCEL_PROTECTION_BYPASS_TOKEN }}" "$HEALTH_URL")
          fi

          # Attempt 3: If still 401, try with Internal API Key
          if [ "$RESPONSE" == "401" ] && [ ! -z "${{ secrets.INTERNAL_API_KEY }}" ]; then
            echo "Bypass header returned 401. Retrying with Bearer token..."
            RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 -H "Authorization: Bearer ${{ secrets.INTERNAL_API_KEY }}" "$HEALTH_URL")
          fi

          if [ "$RESPONSE" != "200" ]; then
            echo "API health check failed with status $RESPONSE"
            exit 1
          fi
          echo "API Health: 200 OK"

          # Content validation (Check for 'ok')
          # Try both auth methods for content validation if needed
          if [ ! -z "${{ secrets.INTERNAL_API_KEY }}" ]; then
            CONTENT=$(curl -s -H "Authorization: Bearer ${{ secrets.INTERNAL_API_KEY }}" --max-time 10 "$HEALTH_URL")
          elif [ ! -z "${{ secrets.VERCEL_PROTECTION_BYPASS_TOKEN }}" ]; then
            CONTENT=$(curl -s -H "x-vercel-protection-bypass: ${{ secrets.VERCEL_PROTECTION_BYPASS_TOKEN }}" --max-time 10 "$HEALTH_URL")
          else
            CONTENT=$(curl -s --max-time 10 "$HEALTH_URL")
          fi
          
          if [[ ! "$CONTENT" =~ "ok" ]]; then
            echo "API content validation failed: 'ok' not found"
            echo "Response: $CONTENT"
            exit 1
          fi
          echo "Content validation: Success"

      - name: Notify Success
        if: success() && env.DISCORD_WEBHOOK != ''
        uses: actions/github-script@v7
        with:
          script: |
            const discordWebhook = process.env.DISCORD_WEBHOOK;
            const targetUrl = context.payload.deployment_status.target_url;
            const repo = context.repo.owner + '/' + context.repo.repo;
            
            const message = {
              content: `✅ **Health Check Passed** for \`${repo}\`\n**URL:** ${targetUrl}\nStatus: 200 OK, Content Validated.`,
            };

            await fetch(discordWebhook, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify(message),
            });
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}

      - name: Notify Failure
        if: failure() && env.DISCORD_WEBHOOK != ''
        uses: actions/github-script@v7
        with:
          script: |
            const discordWebhook = process.env.DISCORD_WEBHOOK;
            const targetUrl = context.payload.deployment_status.target_url;
            const repo = context.repo.owner + '/' + context.repo.repo;
            
            const message = {
              content: `❌ **Health Check Failed** for \`${repo}\`\n**Target URL:** ${targetUrl}\nAction: Check deployment logs immediately.`,
            };

            await fetch(discordWebhook, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify(message),
            });
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
